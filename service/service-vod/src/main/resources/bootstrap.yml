server:
  port: 8003
spring:
  application:
    name: service-vod
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.100
      config:
        ext-config:
          - data-id: aliyun-sdk.yml
            refresh: true
        server-addr: 192.168.1.100
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1042MB